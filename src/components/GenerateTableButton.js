import React from "react"

function GenerateTableButton({capacity}) {

  return (
    // <button onClick={generate}>Run Algorithm</button>
    <p>button capacity is {capacity}</p>
  )
}

export default GenerateTableButton