const capacityDefaults = {
  defaultValue: 5,
  max: 10,
  min: 1,
};

const itemValueDefaults = {
  defaultValue: 10.0,
  max: 50.0,
  min: 0.0,
  step: 0.1,
};

const maxNumOfItems = 10;

export {capacityDefaults, itemValueDefaults, maxNumOfItems};